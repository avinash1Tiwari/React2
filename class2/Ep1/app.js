

const heading = React.createElement("h1",{ id : "header"}," let's add some styles to react created element by adding attributes to it")

const root = ReactDOM.createRoot(document.getElementById("root"))

root.render(heading);
